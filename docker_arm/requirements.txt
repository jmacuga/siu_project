numpy==1.24.1
# rospy→roslib needs rospkg; isolated venv does not see apt python3-rospkg-modules without --system-site-packages.
rospkg>=1.5.0,<2
# Pip NumPy + apt SciPy mix breaks (np.typeDict). Pin SciPy for Py3.8 / NumPy 1.24.
scipy==1.10.1
keras==2.13.1
tensorflow==2.13.1